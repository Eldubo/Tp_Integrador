@model Perro

@{
    ViewData["Title"] = "Añadir Mascota";
}

<h2>Añadir Mascota</h2>

<form asp-action="AñadirMascotaAUsuario" method="post">
    <div class="form-group">
        <label asp-for="Nombre" class="control-label">Nombre</label>
        <input asp-for="Nombre" class="form-control" />
        <span asp-validation-for="Nombre" class="text-danger"></span>
    </div>

    <div class="form-group">
        <label asp-for="Fnac" class="control-label">Fecha de Nacimiento</label>
        <input asp-for="Fnac" class="form-control" type="date" />
        <span asp-validation-for="Fnac" class="text-danger"></span>
    </div>

    <button type="submit" class="btn btn-primary">Añadir Mascota</button>
</form>
@if(ViewBag.Mensaje != null) {
    <p>@ViewBag.Mensaje</p>
}

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
