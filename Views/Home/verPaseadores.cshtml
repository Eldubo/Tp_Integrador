<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Perfil</title>
    <link rel="stylesheet" href="~/css/index.css">
    <link rel="stylesheet" href="~/css/verPaseadores.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" integrity="sha384-k6RqeWeci5ZR/Lv4MR0sA0FfDOMUSc9caGxOnKPz8wG8LIbk5/hd1JiQ+9H6p6zm" crossorigin="anonymous">
    <script src="https://kit.fontawesome.com/878301a14b.js" crossorigin="anonymous"></script>
</head>
<header>
        <div class="top-bar">
            <div class="logo">
                <img src="~/img/logo.png" alt="Huellitas Felices">
            </div>
            <div class="user-icons">
                @if (ViewBag.User != null)
                {
                    <a href="@Url.Action("Perfil", "Home")">Bienvenido @ViewBag.User.Nombre </a>
                    <a href="#"><i class="fa-solid fa-calendar-days" style="color: #2b2b2b;"></i></a>
                }
                else
                {
                    <a href='@Url.Action("Login", "Account")'><i class="fas fa-user"></i></a>
                }
            </div>
        </div>
    </header>

<div class="content">
    <div class="paseadores"> 
        <h1>Paseadores</h1>
        @if (ViewBag.Trabajadores != null)
            {
                foreach (var trabajador in ViewBag.Trabajadores)
                {
                    <div class="paseador">
                        <div id="foto">
                            <img src="~/img/Default_pfp.png" alt="pfp"> 
                        </div>
                        <div id="datos">
                        <h4>Nombre: @trabajador.Nombre</h4>
                        <p>Email: @trabajador.Mail</p>
                        <p>Ciudad: @trabajador.Ciudad</p>
                        <p>Paseo: @(trabajador.Paseo == "true" ? "Sí" : "No")</p>
                        <p>Cuidado: @(trabajador.Cuidado == "true" ? "Sí" : "No")</p>
                        <p>Hola, me llamo Bruno. Amo a los perros y los cuido con cariño y responsabilidad. Cada paseo es un momento de diversión, seguridad y bienestar.</p>
                        <button><a href="@Url.Action()">Ver perfil</a></button>
                        </div>
                    </div>
                }
            }
            else
            {
                <p>No hay trabajadores disponibles.</p>
            }
        <div class="mapa">
            <img src="~/img/mapa.png" alt="Mapa Muestra">
        </div>
    </div>
</div>
